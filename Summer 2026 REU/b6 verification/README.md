# B6 Verification

> Historical verification package only. This folder is separate from the
> frozen 2014-2023 paper-facing ladder and does not supply its headline values.

Chris requested B6 as a frozen 2020 verification package with six cases:

```text
A/B/C architectures x Oracle/Causal
```

Useful files:

| File or folder | Purpose |
| --- | --- |
| `code/B6_CANONICAL_RUNNER.py` | Runs the frozen B6 cases. |
| `code/B6_FINAL_VALIDATE.py` | Validates the B6 outputs and QA checks. |
| `b6_final_results/` | Completed hourly CSVs, logs, run summary, frozen config, and QA summary. |

B6 is not the main paper ladder. It is a reproducibility and validation packet.
