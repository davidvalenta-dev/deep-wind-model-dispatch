# Scripts

This folder contains helper scripts that are not part of the original model training entry points.

| Script | Purpose |
| --- | --- |
| `build_newest_pyron_shaped_dataset.py` | Builds the newest public proxy data in the same column shape as the older Pyron data |

Run scripts from the repo root using the project virtual environment.
